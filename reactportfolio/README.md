# Portfolio (React)

Vite + React + TypeScript app in `reactportfolio/`. The Angular site at the repo root stays the live site until cutover.

```bash
cd reactportfolio
npm install
npm run dev      # http://localhost:5173
npm run build
npm run lint
npm run format
```
